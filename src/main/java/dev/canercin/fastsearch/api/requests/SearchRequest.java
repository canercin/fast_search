package dev.canercin.fastsearch.api.requests;

/**
 * Represents a generic search request in the FastSearch API.
 * This interface serves as a marker for all search request types,
 * allowing for polymorphic handling of different search request implementations.
 * Specific search request types should implement this interface to define their own properties and behaviors.
 */
public interface SearchRequest {
}
